pub fn reverse(input: &str) -> String {
    
    let result = String::new();
    for i in input.len()..0 {
        //result.push(result.chars().nth(i).unwrap());
        println!("{}",result.chars().nth(i).unwrap());
    }
    
    return result;
}
